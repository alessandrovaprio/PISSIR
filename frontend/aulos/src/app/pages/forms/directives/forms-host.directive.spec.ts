import { FormsHostDirective } from './forms-host.directive';

describe('FormsHostDirective', () => {
  it('should create an instance', () => {
    //const directive = new FormsHostDirective();
    //expect(directive).toBeTruthy();
  });
});
