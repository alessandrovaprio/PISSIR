import { FormsDialogHostDirective } from "./forms-dialog-host.directive";

describe('FormsDialogHostDirective', () => {
  it('should create an instance', () => {
    // const directive = new FormsDialogHostDirective();
    // expect(directive).toBeTruthy();
  });
});
